{
  "name": "yumail",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.4.0",
  "scripts": {
    "dev": "pnpm --filter @yumail/desktop tauri:dev",
    "build": "pnpm build:packages && pnpm --filter @yumail/desktop build",
    "build:packages": "pnpm -r --filter './packages/**' build",
    "typecheck": "tsc -b",
    "lint": "eslint .",
    "test": "pnpm build:packages && node --test packages/mail/test/*.test.mjs packages/core/test/*.test.mjs packages/db/test/*.test.mjs packages/renderer/test/*.test.mjs",
    "check": "pnpm typecheck && pnpm lint && pnpm check:boundaries",
    "check:boundaries": "node scripts/check-boundaries.mjs"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^25.9.2",
    "eslint": "^10.4.1",
    "globals": "^17.6.0",
    "typescript": "~5.9.3",
    "typescript-eslint": "^8.60.1"
  }
}
